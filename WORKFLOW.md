# WORKFLOW

flow
- fetch/pull first
- follow formate when creating new
- make sure all new files are added to lib.rs, and mdbook pages linked
- run `cargo check`
- commit/push after making chages and checking

files to create see `docs/SC/hello.sleet.near.md` as an example each smart contract should have an md file and the methods const file, id const file, and the the fun files for the differnet methods, and maybe a near kit typed contract, and bins to test.

questions to ask
- what is the contract id? it is on both networks (if not given duing initail prompt)
- verify the methods by running the near cli inspect  command


==========================
<br/>
copyright 2026 by sleet.near